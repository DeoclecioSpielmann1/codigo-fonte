//Verificação de Elegibilidade
//Crie um código que verifica se uma pessoa pode votar.
//Use a idade como entrada e exiba a mensagem apropriada..
let idade = 20; 
    if (idade >= 18) {
        console.log("Você é elegível para votar.");
    } else {
        console.log("Você não é elegível para votar.");
    }